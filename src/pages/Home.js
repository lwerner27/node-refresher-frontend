import React from "react";

const Home = ({ products }) => {
    return (
        <div className="container">
            <h1>This is the home page.</h1>
        </div>
    );
};

export default Home;
